spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true

security:
  enabled: false

jwt:
  secret: ~
  accessTokenExpires: 0
  refreshTokenExpires: 0


